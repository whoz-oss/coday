# coday-config

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build coday-config` to build the library.

## Running unit tests

Run `nx test coday-config` to execute the unit tests via [Jest](https://jestjs.io).
