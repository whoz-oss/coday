/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http'
import { Injectable } from '@angular/core'
import { Observable } from 'rxjs'
import { map } from 'rxjs/operators'

import { BaseService } from '../base-service'
import { ApiConfiguration } from '../api-configuration'
import { StrictHttpResponse } from '../strict-http-response'

import { generate } from '../fn/ai-controller/generate'
import { Generate$Params } from '../fn/ai-controller/generate'

@Injectable({ providedIn: 'root' })
export class AiControllerService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http)
  }

  /** Path part for operation `generate()` */
  static readonly GeneratePath = '/ai/generate'

  /**
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `generate()` instead.
   *
   * This method sends `application/json` and handles request body of type `application/json`.
   */
  generate$Response(
    params: Generate$Params,
    context?: HttpContext
  ): Observable<
    StrictHttpResponse<{
      [key: string]: string
    }>
  > {
    const obs = generate(this.http, this.rootUrl, params, context)
    return obs
  }

  /**
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `generate$Response()` instead.
   *
   * This method sends `application/json` and handles request body of type `application/json`.
   */
  generate(
    params: Generate$Params,
    context?: HttpContext
  ): Observable<{
    [key: string]: string
  }> {
    const resp = this.generate$Response(params, context)
    return resp.pipe(
      map(
        (
          r: StrictHttpResponse<{
            [key: string]: string
          }>
        ): {
          [key: string]: string
        } => r.body
      )
    )
  }
}
