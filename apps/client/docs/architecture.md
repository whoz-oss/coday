# Angular Client Architecture

Overview of the `apps/client` structure: app shell, feature components, the two-layer service pattern (api / state), guards, and how the client integrates with both the Express backend and AgentOS.
