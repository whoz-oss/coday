# Angular Guidelines

Mandatory Angular patterns for this project: standalone components, `inject()`, `@if`/`@for` control flow, signals for new state, typed forms, and the anti-patterns to actively avoid.
