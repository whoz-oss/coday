{
  "name": "desktop-twin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "{projectRoot}",
  "tags": ["desktop-bundle"],
  "targets": {
    "nx-release-publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Skipping npm publish for desktop-twin - PKG will be uploaded to GitHub Release'"
      }
    },
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "assets": [
          {
            "glob": "**/*",
            "input": "{projectRoot}/assets",
            "output": "."
          },
          {
            "glob": "loader.html",
            "input": "{projectRoot}/src",
            "output": "."
          }
        ],
        "deleteOutputPath": false,
        "external": ["electron"],
        "format": ["cjs"],
        "main": "{projectRoot}/src/main.ts",
        "outputFileName": "main.js",
        "outputPath": "{projectRoot}/dist",
        "platform": "node",
        "thirdParty": true,
        "tsConfig": "{projectRoot}/tsconfig.app.json",
        "esbuildOptions": {
          "outExtension": {
            ".js": ".js"
          }
        }
      }
    },
    "build-preload": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "deleteOutputPath": false,
        "external": ["electron"],
        "format": ["cjs"],
        "main": "{projectRoot}/src/preload.ts",
        "outputFileName": "preload.js",
        "outputPath": "{projectRoot}/dist",
        "platform": "node",
        "thirdParty": true,
        "tsConfig": "{projectRoot}/tsconfig.app.json",
        "esbuildOptions": {
          "outExtension": {
            ".js": ".js"
          }
        }
      }
    },
    "build-all": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node -e \"const fs=require('fs'); ['apps/desktop-twin/dist','apps/desktop-twin/release'].forEach(p=>{if(fs.existsSync(p))fs.rmSync(p,{recursive:true,force:true})})\"",
          "node -e \"require('fs').mkdirSync('apps/desktop-twin/dist',{recursive:true})\"",
          "nx run desktop-twin:build",
          "nx run desktop-twin:build-preload",
          "node -e \"require('fs').copyFileSync('apps/desktop-twin/src/loader.html','apps/desktop-twin/dist/loader.html')\"",
          "node -e \"require('fs').copyFileSync('apps/desktop-twin/src/setup.html','apps/desktop-twin/dist/setup.html')\"",
          "node -e \"require('fs').copyFileSync('apps/desktop-twin/src/preferences.html','apps/desktop-twin/dist/preferences.html')\"",
          "node -e \"const fs=require('fs'); if(fs.existsSync('apps/desktop-twin/assets/icon.png')) fs.copyFileSync('apps/desktop-twin/assets/icon.png','apps/desktop-twin/dist/icon.png')\"",
          "echo Desktop Twin app built successfully"
        ],
        "parallel": false
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-all"],
      "options": {
        "commands": ["electron apps/desktop-twin/dist/main.js"],
        "parallel": false
      }
    },
    "package": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-all"],
      "options": {
        "commands": [
          "electron-builder --dir"
        ],
        "cwd": "{projectRoot}"
      }
    },
    "package-dist": {
      "executor": "nx:run-commands",
      "dependsOn": ["build-all"],
      "options": {
        "commands": [
          "electron-builder"
        ],
        "cwd": "{projectRoot}"
      }
    },
    "package-pkg": {
      "executor": "nx:run-commands",
      "dependsOn": ["package-dist"],
      "options": {
        "commands": [
          "chmod +x apps/desktop-twin/macos/scripts/build-pkg.sh",
          "bash apps/desktop-twin/macos/scripts/build-pkg.sh"
        ],
        "parallel": false
      }
    }
  }
}
