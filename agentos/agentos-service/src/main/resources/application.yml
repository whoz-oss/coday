spring:
  application:
    name: agentos-service
  ai:
    chat:
      client:
        enabled: false
    model:
      chat: none
      audio:
        speech: none
        transcription: none
      image: none
      embedding: none
      moderation: none
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: gpt-4-turbo-preview
          temperature: 0.7
    
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:}
      chat:
        options:
          model: claude-3-5-sonnet-20241022
          temperature: 0.7
    
    mcp:
      client:
        enabled: ${MCP_ENABLED:false}

server:
  port: 8123
  compression:
    enabled: false
  error:
    include-message: always
    include-binding-errors: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when-authorized

agentos:
  plugins:
    dir: ${PLUGINS_DIR:plugins/}
  security:
    # Identity resolution mode: 'local' (default) or 'auth'.
    # local: uses the OS username; auto-creates a User record on first access.
    # auth:  decodes the Cloudflare JWT (CF_Authorization header) or falls back
    #        to x-forwarded-email; requires the User to be pre-registered.
    # Override with env var: AGENTOS_SECURITY_MODE
    mode: local
  persistence:
    # Root directory for persisted JSON files. Relative to the working directory.
    # Override with env var: AGENTOS_PERSISTENCE_DATA_DIR
    data-dir: data/
    # Persistence mode:
    #   filesystem      - JSON files under data-dir, no external dependency
    #   in-memory                - no persistence across restarts, useful for tests
    #   embedded-neo4j           - embedded Neo4j engine, no Docker required;
    #                              database stored under data-dir/neo4j/
    #   neo4j                    - standalone Neo4j server (requires spring.neo4j.* config)
    # Override with env var: AGENTOS_PERSISTENCE_MODE
    # Default is embedded-neo4j: filesystem and in-memory modes are deprecated
    # and will be removed in a future neo4j is the only supported
    # production mode going forward.
    mode: ${AGENTOS_PERSISTENCE_MODE:embedded-neo4j}
  user-group:
    max-users-per-group: ${USER_GROUP_MAX_USERS:30000}
    max-agents-per-group: ${USER_GROUP_MAX_AGENTS:1000}

logging:
  level:
    root: INFO
    io.whozoss.agentos.service: DEBUG
    org.pf4j: DEBUG
    org.springframework.ai: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
