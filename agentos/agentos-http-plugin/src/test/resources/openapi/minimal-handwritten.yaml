openapi: 3.0.0
info:
  title: Minimal
  version: 1.0.0
paths:
  /tickets:
    get:
      summary: List tickets
      responses:
        '200':
          description: Tickets
  /tickets/{ticket_id}:
    get:
      summary: Show a ticket
      parameters:
        - name: ticket_id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Ticket
