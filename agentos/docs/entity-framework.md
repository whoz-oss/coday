# Entity Framework

How entities, services, repositories and controllers are structured and how to add a new one, using the `Entity` → `EntityService` → `EntityRepository` → `EntityController` chain.
