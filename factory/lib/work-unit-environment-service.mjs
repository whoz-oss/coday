import { randomUUID } from 'node:crypto'
const machine=e=>typeof e?.code==='string'&&/^[A-Z][A-Z0-9_]{0,63}$/.test(e.code)?e.code:'GIT_FAILED'
const sameIdentity=(environment,input,id)=>environment.environmentId===id&&environment.workflowId===(input.workflowId??environment.workflowId)&&environment.workUnitId===input.workUnitId&&environment.namespaceId===input.namespaceId&&environment.repoRoot===input.repoRoot&&environment.integrationBranch===input.integrationBranch&&environment.branch===input.branch&&environment.worktreePath===input.worktreePath&&environment.createdBy===input.createdBy
export class WorkUnitEnvironmentService{
 constructor({store,git,clock=()=>new Date(),idGenerator=()=>randomUUID(),fault=async()=>{}}){this.store=store;this.git=git;this.clock=clock;this.idGenerator=idGenerator;this.fault=fault;this.locks=new Map()}
 _locked(ns,id,fn){const k=`${ns}\0${id}`,p=this.locks.get(k)??Promise.resolve(),o=p.then(fn),t=o.catch(()=>{});this.locks.set(k,t);return o.finally(()=>{if(this.locks.get(k)===t)this.locks.delete(k)})}
 async provision(input){const id=input.environmentId??this.idGenerator();return this._locked(input.namespaceId,id,()=>this._provision(input,id))}
 async _provision(input,id){let current=await this.store.read(input.namespaceId,id);if(current){const e=current.environment;if(!sameIdentity(e,input,id))return{ok:false,error:{code:'ENVIRONMENT_IDENTITY_CONFLICT'}};if(e.lifecycleState!=='provisioning')return e.lifecycleState==='error'?{ok:false,error:{code:'ENVIRONMENT_ERROR'}}:{ok:false,error:{code:'INVALID_PROVISION'}};const reconciliation=await this.git.reconcile(e);if(reconciliation.status==='owned'){if(!e.baseCommit)return{ok:false,error:{code:'OWNERSHIP_UNCERTAIN'}};return{ok:true,changed:false,snapshot:current,headCommit:reconciliation.headCommit}}if(reconciliation.status==='uncertain')return{ok:false,error:{code:'OWNERSHIP_UNCERTAIN'}}}
  let reserved=current;let addAttempted=false
  try{const facts=await this.git.provisionWorktree(input,async inspected=>{if(!reserved){const descriptor={schemaVersion:'1',environmentId:id,workUnitId:input.workUnitId,workflowId:input.workflowId,namespaceId:input.namespaceId,...(input.businessRef?{businessRef:input.businessRef}:{}),...(input.businessType?{businessType:input.businessType}:{}),repoRoot:inspected.repoRoot,integrationBranch:inspected.integrationBranch,branch:input.branch,worktreePath:inspected.worktreePath,baseCommit:inspected.baseCommit,createdAt:this.clock().toISOString(),createdBy:input.createdBy,lifecycleState:'provisioning'};const r=await this.store.reserve(descriptor);if(!r.ok)throw Object.assign(new Error('STORE_REJECTED'),{code:'STORE_REJECTED'});reserved=r.snapshot}addAttempted=true});await this.fault('after-git-add',{namespaceId:input.namespaceId,environmentId:id});current=await this.store.read(input.namespaceId,id);if(current.environment.baseCommit!==facts.baseCommit)return{ok:false,error:{code:'OWNERSHIP_UNCERTAIN'}};return{ok:true,changed:false,snapshot:current,headCommit:facts.headCommit}
  }catch(error){current=await this.store.read(input.namespaceId,id);if(current?.environment.lifecycleState==='provisioning'){let reconciliation;try{reconciliation=await this.git.reconcile(current.environment)}catch{return{ok:false,error:{code:'OWNERSHIP_UNCERTAIN'}}}if(reconciliation.status==='owned')return{ok:false,error:{code:'POST_ADD_RECOVERY_REQUIRED'}};if(reconciliation.status==='uncertain')return{ok:false,error:{code:'OWNERSHIP_UNCERTAIN'}};await this.store.transition(input.namespaceId,id,{...current.environment,lifecycleState:'error'},{expectedRevision:current.revision,errorCode:machine(error)})}throw error}}
 async bindParentCase(ns,id,caseId){return this._locked(ns,id,async()=>{const c=await this.store.read(ns,id);if(c?.environment.lifecycleState==='active'&&c.environment.parentCaseId===caseId)return{ok:true,changed:false,snapshot:c};if(!c||c.environment.lifecycleState!=='provisioning'||!c.environment.baseCommit)return{ok:false,error:{code:'INVALID_BIND'}};const existing=await this.store.list(ns,{states:['active']});if(existing.some(snapshot=>snapshot.environment.environmentId!==id&&snapshot.environment.parentCaseId===caseId))return{ok:false,error:{code:'WRITER_ALREADY_ACTIVE'}};if(existing.some(snapshot=>snapshot.environment.environmentId!==id&&snapshot.environment.worktreePath===c.environment.worktreePath))return{ok:false,error:{code:'WORKTREE_ALREADY_ACTIVE'}};return this.store.transition(ns,id,{...c.environment,parentCaseId:caseId,lifecycleState:'active'},{expectedRevision:c.revision})})}
 async inspect(ns,id){return this._locked(ns,id,async()=>{const snapshot=await this.store.read(ns,id);if(!snapshot)return{ok:false,error:{code:'NOT_FOUND'}};if(snapshot.environment.lifecycleState==='removed')return{ok:true,snapshot,reconciliation:{status:'absent'}};const reconciliation=await this.git.reconcile(snapshot.environment);if(reconciliation.status!=='owned')return{ok:false,error:{code:'OWNERSHIP_UNCERTAIN'},snapshot,reconciliation};return{ok:true,snapshot,reconciliation}})}
 async setState(ns,id,state){return this._locked(ns,id,async()=>{const c=await this.store.read(ns,id);return c?this.store.transition(ns,id,{...c.environment,lifecycleState:state},{expectedRevision:c.revision}):{ok:false,error:{code:'NOT_FOUND'}}})}
 async remove(ns,id){return this._locked(ns,id,async()=>{const c=await this.store.read(ns,id);if(!c)return{ok:false,error:{code:'NOT_FOUND'}};if(c.environment.lifecycleState==='removed')return{ok:true,changed:false,snapshot:c};if(!['completed','abandoned','error'].includes(c.environment.lifecycleState))return{ok:false,error:{code:'INVALID_REMOVE'}};await this.git.removeWorktree(c.environment);return this.store.transition(ns,id,{...c.environment,lifecycleState:'removed'},{expectedRevision:c.revision})})}
 async listRecoveryCandidates(ns){return this.store.list(ns,{states:['provisioning','error']})}
}
